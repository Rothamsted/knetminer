QTLNetMiner
===========

QTLNetMiner is a client-server application. Most client code is written in JavaScript/HTML5 and 3rd party data visualisation tools based on Adobe Flash and Java Applet are embedded. The server is written purely in Java making use of the Ondex API (www.ondex.org). It holds a graph based database in memory and communicates to client requests via a Socket connection.
